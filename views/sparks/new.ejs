<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Make a Spark</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/quill@2.0.2/dist/quill.snow.css" rel="stylesheet" />
    <%- include('../partials/head') %>
    
</head>
<body>
    <%- include('../partials/header') %>
    <h1>Make Your Spark</h1>
    <div class="form-wrapper">
        <form action="/sparks" method="POST" enctype="multipart/form-data">
            <div class="row">
                <label for="title">Title of Your Spark</label>
                <input type="text" name="title" id="title" value="<%= fieldValues.title %>">
                <% if (errors.title) { %>
                <p class="error-message"><%= errors.title.message %></p>
                <% } %>
            </div>
            <div class="row">
                <label for="resources">Resources Needed</label>
                <input type="text" name="resources" id="resources" value="<%= fieldValues.resources %>">
                <% if (errors.resources) { %>
                <p class="error-message"><%= errors.resources.message %></p>
                <% } %>
            </div>
            <div class="row">
                <label for="description">Explain Your Spark</label>
                <textarea type="text" name="description" id="description"><%= fieldValues.description %></textarea>
                <% if (errors.description) { %>
                <p class="error-message"><%= errors.description.message %></p>
                <% } %>
            </div>
            <div class="row">
                <label for="variations">Any Variations</label>
                <input type="text" name="variations" id="variations">
            </div>
            <!-- Need to experiment with tags and see what is POSTed when check boxes are used -->
            <!-- Having submitted a string for a dag nothing was sent, do I need to link that to a different form action as it's a different schema. -->
            <div class="row">
                <label for="image">Image of Your Spark</label>
                <input type="file" name="image" id="image" accept="image/*">
            </div>
            <% if (errors.message) { %>
                <p class="error-message"><%= errors.message %></p>
            <% } %>   
            
            <div class="row">
                <label for="tag">Add Tags</label>
                <input type="text" name="tag" id="tag">
            </div>
            <% if (errors.message) { %>
                <p class="error-message"><%= errors.message %></p>
            <% } %>      
            <div class="btn-row">
                <button class="form-btn" type="submit">Share Your Spark</button>
            </div>
        </form>
    </div>
</body>
<!-- Link to Scripts -->
    <script src="/scripts/scripts.js"></script>
</html>